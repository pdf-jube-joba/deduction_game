[package]
name = "game-core"
version = "0.1.0"
edition = "2021"

[dependencies]
itertools = "0.13.0"
rand = { version = "0.8.5", default-features = false, features = ["alloc", "std_rng"] }
serde = { version = "1.0", features = ["derive"] }
