[package]
name = "game-ai"
version = "0.1.0"
edition = "2021"

[dependencies]
game-core = { path = "../game-core" }
rand = { version = "0.8.5", default-features = false, features = ["alloc", "small_rng"] }
